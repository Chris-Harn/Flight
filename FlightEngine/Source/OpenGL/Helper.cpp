#include "OpenGL/Helper.h"

void DisplayPerformanceInformation() {
    //// Display Frame Rate
    //if( m_bDisplayText == true ) {
    //    int count = m_pTimer->GetFPS();
    //    std::string CurrentFrame = "Display Frequency: " + std::to_string( count ) + "\nVSynch: " + ( ( m_pMainWindow->GetVsync() == true ) ? "True" : "False" ) + "\nCap FPS: ";
    //    CurrentFrame += ( ( m_pTimer->GetRegulationValue() == true ) ? "True" : "False" );
    //    CurrentFrame += "\nFrame budget: 16.67 ms\nFrame Time: " + std::to_string( 1000.0f / count ) + " ms";
    //    m_pTextRenderer->DrawTextString( CurrentFrame, 25.0f, (float)( m_pMainWindow->GetBufferHeight() - 25.0f ), 0.5f, 0.9f, 0.9f, 0.9f );
    //}
}
